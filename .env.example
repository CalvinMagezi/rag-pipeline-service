# RAG Pipeline Service Configuration
# Copy this file to .env and configure with your values

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=8888
HOST=0.0.0.0

# ===========================================
# EMBEDDING PROVIDER CONFIGURATION
# ===========================================
# Choose one: 'openai' or 'gemini'
EMBEDDING_PROVIDER=gemini

# Gemini Configuration (Google AI)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=text-embedding-004
# Note: Gemini uses 768 dimensions

# OpenAI Configuration (Alternative)
OPENAI_API_KEY=your_openai_api_key_here
EMBEDDING_MODEL=text-embedding-3-small
# Note: OpenAI uses 1536 dimensions

# Embedding Dimensions (auto-set based on provider)
# Gemini: 768, OpenAI: 1536
EMBEDDING_DIMENSION=768

# ===========================================
# VECTOR STORE CONFIGURATION
# ===========================================
# Choose one: 'in-memory', 'filesystem', or 'postgres'
VECTOR_STORE_PROVIDER=filesystem

# Filesystem Vector Store (default)
VECTOR_STORE_PATH=./data/vectors

# PostgreSQL Vector Store (alternative - uncomment to use)
# VECTOR_STORE_PROVIDER=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=raguser
# POSTGRES_PASSWORD=ragpassword
# POSTGRES_DB=ragdb
# POSTGRES_TABLE=vectors
# POSTGRES_MAX_CONNECTIONS=10

# ===========================================
# DOCUMENT PROCESSING CONFIGURATION
# ===========================================
CHUNKING_STRATEGY=recursive
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# ===========================================
# QUERY CONFIGURATION
# ===========================================
QUERY_TOP_K=5
QUERY_MIN_SCORE=0.7

# ===========================================
# DOCKER CONFIGURATION
# ===========================================
# These ports are used in docker-compose.yml
# Web App Port: 3333
# RAG API Port: 8888

# ===========================================
# DEVELOPMENT CONFIGURATION
# ===========================================
# Uncomment for development mode
# NODE_ENV=development
# PORT=3000
